# v0.6.3 browser candidate provenance

These files are preserved reconstruction evidence from the pre-publication v0.6.3 browser candidate.

They were relocated unchanged from the public `v0.6.3/` namespace after the reconstructed v0.6.3 package became canonical. They are not current runtime code, public release authority, or supported versioned browser surfaces.

The maintained `v0.6.3/` browser namespace now contains only the current release landing page and intentional redirect surfaces. Current browser tools live at their unversioned public routes and consume the canonical `releases/v0.6.3/` package.

The candidate files below retain their original Git blob contents for reconstruction provenance only.
