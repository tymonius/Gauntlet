# Clean v0.6.3 Faction Pages validation status

Status before merge: **candidate**.

The dedicated gate must prove that all six runtime sources are exact members of the certified complete clean-v0.6.3 authority set; their SHA-256 hashes match both the complete authority set and faction-authority manifest; the shared clean Browser Rulebook renderer/presentation baselines remain hash-pinned; every route is noindex and analytics-free; the adapter performs raw-byte hash verification before rendering; the six guides render successfully without introducing player-facing gameplay prose outside those sources; the public v0.6.1 faction routes and current-release pointer remain untouched; publication stays locked; and the diff remains confined to this reconstruction, its gate, the required analytics exclusions, and dependency-trigger handling for the already-merged Card Reference gate.
