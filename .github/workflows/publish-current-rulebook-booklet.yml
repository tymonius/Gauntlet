name: Publish current Rulebook booklet

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish-booklet:
    uses: ./.github/workflows/build-current-rulebook-booklet.yml
    with:
      publish: true
