name: Apply v0.6.2 additional Action correction

on:
  pull_request:
    branches:
      - main

concurrency:
  group: apply-v062-additional-action-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  superseded:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by the existing v0.6.2 publication workflow."
