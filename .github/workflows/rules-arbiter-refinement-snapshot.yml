name: Rules Arbiter refinement snapshot

on:
  push:
    branches:
      - rules-arbiter-review-58c9c1c6
    paths:
      - '.github/workflows/rules-arbiter-refinement-snapshot.yml'

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Normalize review diff
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          p = Path('rules-assistant/local-search.js')
          text = p.read_text()
          old = '''  {
          pattern: /(?=[\\s\\S]*\\btactics?\\b)(?=[\\s\\S]*\\b(?:two|2|second|additional|multiple|several)\\b)(?=[\\s\\S]*\\b(?:choose|chooses|chosen|choosing|choice|select|selection)\\b)/i,
          aliases: ["Multiple Gambits or Tactics", "several Tactics chosen as part of the same choice", "choose them simultaneously", "Choose Tactics", "Reveal Tactics"]
        },'''
          new = '''  {
            pattern: /(?=[\\s\\S]*\\btactics?\\b)(?=[\\s\\S]*\\b(?:two|2|second|additional|multiple|several)\\b)(?=[\\s\\S]*\\b(?:choose|chooses|chosen|choosing|choice|select|selection)\\b)/i,
            aliases: ["Multiple Gambits or Tactics", "several Tactics chosen as part of the same choice", "choose them simultaneously", "Choose Tactics", "Reveal Tactics"]
          },'''
          if old not in text:
              raise SystemExit('Expected alias block not found')
          p.write_text(text.replace(old, new, 1))
          PY

          git checkout origin/main -- .github/workflows/rules-arbiter-refinement-snapshot.yml
          rm -f .github/workflows/rules-arbiter-format-fix.yml
          rm -f .github/workflows/rules-arbiter-targeted-inspect.yml

      - name: Commit cleanup
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add rules-assistant/local-search.js .github/workflows/rules-arbiter-refinement-snapshot.yml .github/workflows/rules-arbiter-format-fix.yml .github/workflows/rules-arbiter-targeted-inspect.yml
          git diff --cached --check
          git commit -m "Normalize Rules Arbiter review formatting"
          git push origin HEAD:rules-arbiter-review-58c9c1c6
