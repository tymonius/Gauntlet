name: Temporary Faction Woodcut Hashes

on:
  pull_request:

permissions:
  contents: read

jobs:
  hashes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print faction woodcut SHA-256
        run: sha256sum images/woodcuts/factions/*.png
