name: gauntlet-newsletter

x-db-credentials: &db-credentials
  POSTGRES_USER: ${POSTGRES_USER:-listmonk}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
  POSTGRES_DB: ${POSTGRES_DB:-listmonk}

services:
  app:
    image: listmonk/listmonk:v6.2.0
    container_name: gauntlet_listmonk
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    command:
      - sh
      - -c
      - >-
        ./listmonk --install --idempotent --yes --config '' &&
        ./listmonk --upgrade --yes --config '' &&
        ./listmonk --config ''
    environment:
      LISTMONK_app__address: 0.0.0.0:9000
      LISTMONK_db__host: db
      LISTMONK_db__port: 5432
      LISTMONK_db__user: ${POSTGRES_USER:-listmonk}
      LISTMONK_db__password: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      LISTMONK_db__database: ${POSTGRES_DB:-listmonk}
      LISTMONK_db__ssl_mode: disable
      LISTMONK_db__max_open: 25
      LISTMONK_db__max_idle: 25
      LISTMONK_db__max_lifetime: 300s
      LISTMONK_ADMIN_USER: ${LISTMONK_ADMIN_USER:-}
      LISTMONK_ADMIN_PASSWORD: ${LISTMONK_ADMIN_PASSWORD:-}
      TZ: ${TZ:-America/New_York}
    ports:
      - "127.0.0.1:${LISTMONK_PORT:-9000}:9000"
    volumes:
      - listmonk-uploads:/listmonk/uploads
    networks:
      - newsletter

  db:
    image: postgres:17-alpine
    container_name: gauntlet_listmonk_db
    restart: unless-stopped
    environment:
      <<: *db-credentials
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 6
    volumes:
      - listmonk-data:/var/lib/postgresql/data
    networks:
      - newsletter

networks:
  newsletter:

volumes:
  listmonk-data:
  listmonk-uploads:
