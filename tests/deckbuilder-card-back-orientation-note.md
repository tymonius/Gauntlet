# Deckbuilder card-back orientation contract

The canonical production card back already rotates its wordmark 90 degrees in `card-design/card-back.css`. Territory faces are rotated 90 degrees into portrait print slots by the Deckbuilder print compositor. Therefore the Deckbuilder must not rotate the entire standard back surface an additional 180 degrees for duplex output.
