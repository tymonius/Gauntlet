Observed symptom: the Leader slot in a Deckbuilder print package could show `Current Leader card copy failed to load` while other production-rendered cards completed normally. Cause: the Leader-copy overlay in a one-Leader component print iframe waited for the entire twelve-Leader hidden catalog before marking copy ready. Fix: target only the requested print Leader in component print mode; preserve full-catalog behavior elsewhere.
