// Explicit exceptions for genuinely generic/data-driven v0.7.0 digital behavior.
//
// Do not use this registry to bless a card merely because its canonical text is
// read at runtime. A generic proof must identify the maintained runtime source,
// regression coverage, and stable implementation anchors that demonstrate the
// behavior independently of the card's canonical text.
export const V070_GENERIC_DIGITAL_BEHAVIOR_PROOFS = Object.freeze([]);
